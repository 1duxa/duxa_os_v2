[package]
name = "physical-allocator"
version = "0.1.0"
edition = "2024"

[dependencies]
mem-info = { path = "../mem-info"}
constants = { path = "../constants"}
addr = { path = "../addr"}

[lib]
crate-type = ["lib"]
